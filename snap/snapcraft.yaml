name: hommyn-daemon # you probably want to 'snapcraft register <name>'
base: bare
build-base: core18
version: "1.0"
summary: Hommyn smarthome daemon
description: Hommyn smarthome daemon
grade: stable
confinement: strict
parts:
  hommyn-daemon:
    source: https://reacthome.net/download/hommyn/daemon/hommyn-daemon-1.0.tar.gz
    plugin: autotools
